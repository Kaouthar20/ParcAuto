{# {% extends 'base.html.twig' %}

{% block title %}Hello ProductController!
{% endblock %}

{% block body %}
	<div class="container mt-5">
		<table class="table table-striped table-hover">
			<thead class="bg-info">
				<tr>
					<th>ID</th>
					<th>Designation</th>
					<th>quantite</th>
					<th>action</th>

				</tr>
			</thead>
			<tbody>
				{% for p in products %}
					<tr>
						<td>{{p.id}}</td>
						<td>{{p.designation}}</td>
						<td>{{p.qte}}</td>
						<td>
							<a href="{{path('print_product')}}" class="btn btn-primary">print</a>
						</td>

					</tr>

				{% endfor %}

			</tbody>
		</table>
	</div>

{% endblock %} #}
